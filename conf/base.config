process {
  executor = 'local'
  cpus = { params.max_cpus ?: 4 }  // χρησιμοποίησε το --max_cpus ή 4 αν δεν δοθεί
  memory = { params.max_memory ?: 16.GB }
}

conda {
  enabled = true
  cacheDir = './.conda_cache'
}
